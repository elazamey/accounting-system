#!/bin/bash

# ===== نظام تشغيل نظام المحاسبة المتكامل =====
# MiniMax Agent

echo "🚀 بدء تشغيل نظام المحاسبة المتكامل..."

# التحقق من وجود Python
if command -v python3 &> /dev/null; then
    echo "✅ تم العثور على Python 3"
    echo "🌐 بدء الخادم المحلي على المنفذ 8000..."
    echo "📱 افتح المتصفح واذهب إلى: http://localhost:8000"
    echo "🛑 اضغط Ctrl+C لإيقاف الخادم"
    echo "=================================="
    python3 -m http.server 8000
elif command -v python &> /dev/null; then
    echo "✅ تم العثور على Python"
    echo "🌐 بدء الخادم المحلي على المنفذ 8000..."
    echo "📱 افتح المتصفح واذهب إلى: http://localhost:8000"
    echo "🛑 اضغط Ctrl+C لإيقاف الخادم"
    echo "=================================="
    python -m http.server 8000
else
    echo "❌ لم يتم العثور على Python"
    echo "📋 يرجى تثبيت Python أو فتح الملف index.html مباشرة في المتصفح"
    echo ""
    echo "💡 طرق التشغيل البديلة:"
    echo "   1. افتح index.html في المتصفح مباشرة"
    echo "   2. استخدم خادم ويب محلي آخر"
    echo "   3. استخدم أدوات التطوير في VS Code"
    
    # محاولة فتح المتصفح
    if command -v xdg-open &> /dev/null; then
        echo ""
        echo "🌐 محاولة فتح index.html في المتصفح..."
        xdg-open index.html
    elif command -v open &> /dev/null; then
        echo ""
        echo "🌐 محاولة فتح index.html في المتصفح..."
        open index.html
    fi
fi

echo ""
echo "📞 للدعم والمساعدة:"
echo "   • راجع ملف README.md"
echo "   • تأكد من تفعيل JavaScript في المتصفح"
echo "   • تأكد من مساحة تخزين كافية"
echo ""
echo "🎉 شكراً لاستخدام نظام المحاسبة المتكامل!"
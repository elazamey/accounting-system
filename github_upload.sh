#!/bin/bash

echo "🚀 رفع النظام المحاسبي إلى GitHub"
echo "=================================="

# التحقق من وجود Git
if ! command -v git &> /dev/null; then
    echo "❌ Git غير مثبت. يرجى تثبيت Git أولاً من https://git-scm.com"
    exit 1
fi

# تهيئة Repository
echo "📦 تهيئة Repository..."
git init

# إضافة جميع الملفات
echo "📁 إضافة الملفات..."
git add .

# عمل commit أولي
echo "💾 حفظ التغييرات..."
git commit -m "Initial commit - نظام المحاسبة المتكامل

- نظام محاسبة شامل بـ Python Flask
- واجهة عربية RTL
- إدارة العملاء والموردين
- نظام نقاط البيع (POS)
- إدارة المخزون
- تقارير شاملة"

# ربط بـ GitHub (استبدل YOUR_USERNAME باسم المستخدم الصحيح)
echo "🔗 ربط بـ GitHub..."
echo "⚠️  يرجى استبدال YOUR_USERNAME باسم المستخدم الصحيح في الأمر التالي:"
echo "git remote add origin https://github.com/YOUR_USERNAME/accounting-system.git"
echo ""
echo "ثم شغل الأوامر التالية:"
echo "git branch -M main"
echo "git push -u origin main"
echo ""
echo "✅ الأوامر جاهزة للتشغيل!"